# Deprecations the quarterly drift check should stop failing on.
#
# One extended regular expression per line. Blank lines and lines starting with #
# are ignored. A line here means someone decided to live with the deprecation, so
# record who decided, when, and why, directly above it. Remove the line when the
# decision changes and the check will start reporting it again.

# qplot() stays. It was deprecated in ggplot2 3.4.0 but is present and working in
# 4.0.x, and it survived the 4.0 major version. It exists so the first two R
# workshops can draw a plot in one line, without the aes()/+/layers apparatus that
# R Graphics teaches later. Decided by Steve, 2026-08-31. The page carries a note
# explaining the warning, because learners meet it in their own console even though
# the site hides it.
qplot\(\).{0,3}(was|is) deprecated
