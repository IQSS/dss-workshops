[project]
name = "dss-workshops"
version = "0.1.0"
description = "Python environment for rendering the Python workshop pages (Quarto's Jupyter engine)."
requires-python = ">=3.14"
dependencies = [
  "jupyter",
  "ipykernel",
  "numpy",
  "pandas",
  "requests",
  "lxml",
]
