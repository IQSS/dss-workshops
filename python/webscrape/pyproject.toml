[project]
name = "dss-python-webscrape"
version = "2026.8"
description = "Python and the packages for the DSS workshop; run `uv sync` in this folder."
requires-python = ">=3.14"
dependencies = ["ipykernel", "pandas", "requests", "lxml"]
